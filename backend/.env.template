SECRET=clave_secreta_para_jwt
DB_PROVIDER=postgres
DATABASE_URL=postgresql://usuario:password@host.neon.tech/neondb?sslmode=require
DB_USER=usuario
DB_PASS=password
DB_HOST=host.neon.tech
DB_NAME=neondb

# Opcional en producción: CI suele exportar GITHUB_SHA → GIT_COMMIT_SHA.
# GITHUB_REPOSITORY=owner/repo genera enlace a GitHub. En local, si hay .git, se infiere con git.
GIT_COMMIT_SHA=
GITHUB_REPOSITORY=
